pymupdf>=1.24
requests>=2.31
python-dotenv>=1.0

# Toàn bộ pipeline chỉ cần 3 gói trên. Ảnh minh hoạ (sơ đồ tư duy) được vẽ
# THUẦN CODE ra file .svg (mindmap_svg.py) — 0 token, không cần Playwright/
# Chromium hay bất kỳ dependency ngoài nào.
